"""Numerical-results simulation suite (v2) for Section V of the manuscript.

This package only *consumes* the existing ``system_model`` and
``algorithms`` packages -- nothing here is re-derived math, it is glue
code that drives the existing solvers for the Section V figures.
"""
